# Recommended runtime: Python 3.11 (code is 3.9-compatible).
# Core orchestrator
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0

# Perception
onnxruntime>=1.17
numpy>=1.26
opencv-python>=4.9

# Calibration / eval
scipy>=1.12
scikit-learn>=1.3      # the modest local classifier (LogisticRegression)
skl2onnx>=1.16         # export the classifier to ONNX
onnx>=1.15
Pillow>=10.0           # image feature extraction (eval/features.py)

# Actuation (USB relay)
pyserial>=3.5

# Cloud MCP client
mcp>=1.0
tenacity>=8.2          # exponential backoff on cloud 429/5xx

# Tests
pytest>=8.0
