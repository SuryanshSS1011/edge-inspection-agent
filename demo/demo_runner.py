"""Scripted live demo (build plan §8): good part -> local pass; ambiguous part ->
escalate -> cloud diagnosis -> relay rejects; then CUT THE NETWORK live (offline,
conservative reject); reconnect and show the outbox sync. Lands in M8.

Record early — physical demos fail live.
"""


def run_demo(config_path: str = "config.yaml") -> None:  # M8
    raise NotImplementedError
