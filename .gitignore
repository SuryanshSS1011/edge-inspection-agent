__pycache__/
*.pyc
.pytest_cache/
.venv/
edge.db
models/*.onnx
models/temperature.json
# Ship the deployable models in-repo (all well under GitHub's 100MB/file limit) so the
# runtime is reproducible with no external download. DINOv2 is the core backbone; the
# handcrafted + MobileNet heads stay as ablation arms.
!models/classifier.onnx
!models/classifier_dinov2.onnx
!models/dinov2.onnx
!models/dinov2.onnx.data
!models/grid_mobilenet.onnx
!models/temperature_dinov2.json
.env
env
*.egg-info/
data/
demo/SCRIPT.md
# Licensed datasets (MVTec AD / AD 2 / 3D-AD) — never commit, downloaded per-license.
*.tar.xz
*.tar
mvtec_anomaly_detection*
mvtec_ad_2*
mvtec_3d*
datasets/
eval/results.json
eval/fixtures/
models/mobilenetv2.onnx
models/splits.json
models/*.splits.json
.DS_Store

# Internal / not for public display (build plans, deployment guides, dev instructions).
# docs/ holds the build+implementation plans, hackathon PDF, and SAS deployment guide.
docs/
CLAUDE.md
